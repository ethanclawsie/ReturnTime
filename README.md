# ReturnTime
A program that monitors stock prices and records data when a stock reaches a specified target price. To add companies, write them in the text file using the following format: (TICKER, up/down, percent change). Example: (RIVN, up, 10%). You can dynamically add tickers to the txt file and change how frequently it gets the data.
